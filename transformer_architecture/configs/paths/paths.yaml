root_dir: .
data_root_dir: ${paths.root_dir}/data
run_date: ${now:%Y-%m-%d}
run_version: ${next_daily_version:${paths.run_date}}
run_dir: ${paths.root_dir}/logs/${paths.run_date}/v${paths.run_version} # where all the logs go 

#YOU NEED THIS IN YOUR CODE TO GET THE PATHS:

# def _next_daily_version(date_str: str) -> int:
#     """Return next `vN` index for a given date folder inside `outputs/`.

#     Example date folder: outputs/2026-03-16/v1, v2, ...
#     """
#     base_dir = Path("outputs") / date_str
#     if not base_dir.exists():
#         return 1

#     version_pattern = re.compile(r"^v(\d+)$")
#     max_version = 0
#     for child in base_dir.iterdir():
#         if not child.is_dir():
#             continue
#         match = version_pattern.match(child.name)
#         if match:
#             max_version = max(max_version, int(match.group(1)))

#     return max_version + 1